movies#show
.test
  情報投稿者
  = @movie.user.name
.test
  メッセージ投稿者アイコン
  = image_tag src= "#{@movie.user.image}", size: '100x100'
.test
  タイトル
  = @movie.title
  = image_tag @movie.image.url, class: 'Message__image' if @movie.image.present?
.test
  投稿日
  = @movie.created_at.strftime("%Y年%m月%d日")
.test
  あらすじ
  = @movie.summary
.test
  監督
  = @movie.director
.test
  原作
  = @movie.original
.test
  出演
  = @movie.actor

- if current_user
  =form_with(model: [@movie, @comment], local: true) do |f|
    = f.text_area :text, placeholder: 'コメントする', rows: '2'
    = f.submit 'comment'
- else
  コメントの投稿には新規登録/ログインが必要です。

- @comments.each do |comment|
  =comment.user.name
  =image_tag src="#{comment.user.image}", size: '100x100'
  =comment.text
  =comment.created_at.strftime("%Y年%m月%d日")

.return
  =link_to root_path do
    topReturn
  = link_to edit_movie_path(@movie.id) do
    映画情報を編集